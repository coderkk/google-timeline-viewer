# Retros — 2026-09-19

> 每日文件：同一天多次检讨追加 `## HH:mm — 主题` 节。

## 23:55 — T44/T45/T46 质量收尾批

> 范围：v1.0.0 发布后的横批次 Fix 链——T44（merge 页布局）、T45（冒烟纪律补布局核对）、T46（内容页水平居中）。三家 repo 同步、250 测试 + 4 skip 基线、发布链 `v1.0.0` tag 已推远端。

### 做了什么
- **T44** 左边栏 merge 布局修复：`Layout.tsx` `isMapPage` 把 `/app/merge` 误判为地图全屏页（`startsWith('/app')` 一刀切）→ padding 0 / 无居中 / 无 footer；+ smoke-merge-layout 回归脚本 + Windows 服务清理加固（复盘 09-15 §A10）。Reviewer 两次 REJECT 后 PASS
- **T45** T44 教训制度落地：SMOKE-CHECKLIST 补 **D-1 布局核对**（双视口几何互等 + 路由容器语义 + 居中 + overflowX）+ WORKFLOW 规则 5 类别定义补「D 类含新增页面/路由的布局核对」+ 另例（不因 L1 豁免）——公司层/项目层/模板三层联动
- **T46** 用户实测反馈（merge/guide/settings 没居中）→ 根因 `.page-help`/`.settings-page`/`.merge-page` `max-width: 780px` 缺 `margin: 0 auto` → 三处各加 margin auto；smoke 断言升级：三页几何互等 + **每页水平居中 |center−vpCenter|≤1** + 每页 overflowX；**22/22 PASS exit 0**（desktop center 720==720 真居中 / mobile center 195==195）；截图 4 张存档

### 什么做得好（保留）
- **T45 纪律第一次实战就拦到真缺陷（T46）**：D-1 布局核对上线前，T44 只断言 merge==settings「互等」——两页同偏左故 PASS，漏掉居中。D-1 强制「居中」后，T46 用户实测反馈同一个洞，冒烟脚本 22/22 数学坐实居中。**教训 → 纪律 → 实战拦截**闭环在 3 张卡内走完（09-14 的 T44 教训在 T46 兑现）
- **Reviewer 建议全消费 + 主动补漏**：T46 审查抓到 ①截图脚本泄漏 vite pid 44636（需 taskkill 清）②TASKS `next:` 指针滞后 → 撞号 ③overflowX 只测最后停留路由——三项全消费（进程已清 / next→T47 / overflowX 逐页化）——Reviewer 不只是门禁，是补「断言覆盖盲区」的第二双眼睛
- **「残留检查」纪律起效**：每个冒烟轮次后 query vite 进程计数 —— 本轮直接从轮内脚本的 pid 残留捕获（diagnosed 而非盲放）

### 什么做得不好（要改）
- **T44 断言只验了互等没验居中（Reviewer 也放行）**：`merge == settings` 通过不代表页面「在中间」——两页同偏左时互等恒真。**根因：T44 断言停留在「相对一致」，未查「与视口的绝对关系」**——D-1 首个版本里本来该有居中，但 T45 是 T44 教训的产物，D-1 直到 T46 才被补上「居中」这一维度（09-19 23:58 DECISIONS）。模式：**布局断言必须包「绝对锚点」（视口中心/视口宽），不能只在同族之间互比**
- **临时截图脚本泄漏 vite 进程**：shot-t46.mjs 从 TEMP 目录跑、结束时残留 vite preview（pid 44636）——违反 09-15 A10「脚本入库 + 结清」纪律的变体（入库的 smoke 脚本干净，临时验证脚本裸奔）。**根因：验证脚本「入库 vs 临时」两套路径，只有入库那套管了清理**
- **发布链 × 内容页存在「第二处同族」风险**：merge 修好了，但 help/settings 是同一根因（同 `max-width: 780px` 无 margin auto）——T44 只修 merge 一路（用户只报了 merge），T46 靠用户再次反馈才带上 help/settings。**模式：布局类缺陷应同族扫描（grep 同 pattern 全量覆盖），不是等用户逐个点**

### 下次怎么改（行动项）
- [ ] **A17 布局断言必须含绝对锚点**：冒烟布局核对（D-1）断言升级为「每页几何 + 与视口绝对关系（水平居中 / 全宽语义 / 无 overflowX）」，**禁止只断言同族互等**。验收：Reviewer 见任何「== 另一页」形式的布局断言之类 → REJECT 并要求补绝对锚点
- [ ] **A18 临时验证脚本也走清理纪律**：TEMP 目录的临时脚本（截图/探针）须自带 vite 清理（或在任务卡 NOTES 记 pid + reviewer 时核对残留）；临时脚本不再裸奔。验收：下一批任何临时脚本轮次结束，残留 vite 进程 0
- [ ] **A19 布局类缺陷同族扫描前置**：用户报某个布局问题 → 修前先 `grep` 同根因 pattern 全量（本轮 grep `max-width: 780px` 无 margin 命中 merge/help/settings 三处），同族一次修完 + 断言覆盖全族。验收：T46 后续无同根因布局缺陷再报
- [ ] **修正历史工艺**：T44 卡注明「只断言互等未断言居中，D-1 教训」+ DECISIONS 补 T46 验收条目（已做 09-19 23:58）

### 流程健康核对（A16 敏捷护栏）

- **平均卡周期**：T44 09-18→09-19 / T45 09-19→09-19 / T46 09-19→09-19 —— 全部 < 当日完成（小步快跑成立）。基准：09-16 轮发布批最长卡 09-18→09-19（T44）。本轮全部 ≤1 天，**优于基准**
- **规则死重淘汰**：09-16 轮立过 A13（L1 冒烟豁免例外条款）——本轮 T46 正是 L1 冒烟豁免的边界案例（CSS 3 行但**影响布局**，D-1 布局核对照跑无误）。**A13 例外条款本轮被实战引用**：虽 L1（3 行 CSS）但 D 类布局冒烟执行了 —— 规则存活、非死重
- **恒定开销**：本轮卡开销 = lint/test/build（~1min）+ smoke 双视口（每视口 ~30s）+ Reviewer 往返（~2-5min）≈ 每卡 ~5min / 卡。对比总周期（当日完成）——**占比健康，无超 10% 预算线**
- **门禁收益净值**：Reviewer 本轮抓 3 条（T46 截图脚本 pid 泄漏 / next 指针撞号 / overflowX 盲区）+ 09-16 A10（vite 残留检查）在 T46 兑现——**门禁拦截 ≥2 真缺陷（含 pid 泄漏），净值 ≥ 门禁自身开销**。历史矛盾率 0（无回退）

### 复盘 L1 判定偏差率
- **T46 判 L1（CSS 3 行）实为「布局语义变更」**：按 A13 例外条款，虽改动量档位 L1，但**冒烟义务不豁免**（D-1 布局核对执行）。Dev 判定 L1 ✓ 且执行了完整冒烟 —— **判定与执行一致，无偏差**
- **偏差率**：0 / 3（本轮无「升档」或「降档」争议）——T44 首轮的争议（客户是否将该判 L2）未再出现；「L1 豁免 Session 性回归义务、不豁免发布门禁与矩阵」A13 落地后，判档争议归零

---
**参与**: CEO 主持，Dev + Reviewer 反馈。整理: CEO 2026-09-19 23:55。
