# Retros — 2026-09-15

> 每日文件：同一天多次检讨追加 `## HH:mm — 主题` 节。

## 13:51 — OPC 流程检讨（T16–T30，google-timeline-viewer 阶段复盘）

### 做了什么
- T16–T30 共 13 个 commit / ~15 个任务批次：时间轴视图、语义段识别、GPS 缝合、跨平台 i18n、UI/UX 精修批（T30）等；NOTES 累计 14 条 Reviewer 记录。
- 期间事件：T16-T17 返工 4 轮（PRD 补写，已记 2026-09-14 retro）；T27/T28 四轮往返（含 `map.stop()` 白屏致命回退）；T29 两轮；T30 一次过但「过审前已 commit」违反规则 11。

### 什么做得好（保留）
- **真实数据端到端验证**（Dev）：livedata 一轮 > 单测十轮——渲染/UI 类任务用真档一测就找到根因（T16-T17 timelinePath time 丢弃、T23 long task）。
- **Reviewer 门控实际拦截真 bug**（Dev/Reviewer 共识）：T13.6 死代码链路、T22 跨午夜裁剪漏 activityType、T27 `map.stop()` 白屏、`>=2`/`>0` 口径问题——都是上线即功能缺陷。
- **需求对齐规则（#3）后半段起效**：T20 之后返工多为「Reviewer 抓真 bug」，不再是「做错方向重来」。
- **时区「两轮咨询、允许翻覆」**（Designer）：UX 意见进决策流、且能被数据推翻——机制本身正确，流程保留。

### 什么做得不好（要改）
- **浏览器冒烟无标准清单**：每次从脑中随机抽验 → 漏项（T30 漏 popover 焦点陷阱）、重复验证（T16-17 多次全量 reload）。
- **fallback 门槛口径重复犯**：`path.length >= 2` vs `>0` 同一 pattern 在 T20-25 与 T27 各中一次，浪费 ≥2 轮 Reviewer 周期。
- **PRD 先行未机制化**：规则 2 存在但无 TASKS 层强制（T29 仍现 A1/A3 PRD 与实作不符）；「已知限制 vs 必改」判定成本全压 Reviewer。
- **Dev 遇 PRD-实作冲突自行消化**（T14.2→T14.3 双闸门撤销本质是产品决策，不应在代码里「顺便」做）。
- **插件唤起时机前后不一**（Designer）：T24 移动端、T30 popover 纯 UI 任务未挂 Designer；时区数据语义问题却咨询两轮。
- **规则 11 执行漏洞**：T30 Dev 在 Reviewer 过审前 commit（已记 NOTES，流程文字需补明确）。

### 下次怎么改（行动项，CEO 已拍板采纳）
- [x] **A1 冒烟清单模板**：新增 `templates/project/docs/SMOKE-CHECKLIST.md`（按任务类型 A/B/C/D 列必验项）；Dev 提交审查须附冒烟勾选结果，缺则 Reviewer 可打回（WORKFLOW #5 已更新，DEV/REVIEWER persona 已同步）
- [x] **A2 PRD 触达条款**：拆 TASKS 时每 Task 引用 `来源: PRD#功能N` + 验收条件，Reviewer 照单核对；Dev 遇 PRD-实作冲突暂停回报 CEO，不自行拍板（WORKFLOW #2 + DEV persona 已更新）
- [x] **A3 口径三方对照**：REVIEWER.md 审查清单固化「计算口径 vs 渲染器 vs PRD」对照；code 部分（抽 `hasRenderablePath`/`MIN_PATH_LEN` 共用常量）→ 记入 google-timeline-viewer Backlog（T31 候选）
- [x] **A4 规则 11 硬化**：未经过审的代码不 commit 进项目历史（仅本地/wip）；过审后 Dev commit、CEO 统一 push（WORKFLOW #11 已更新）
- [x] **A5 插件挂载机制**：UI subtask 拆 TASKS 即自动挂 Designer；咨询附「用户原话 + PRD 段 + 数据实证」输入包（WORKFLOW + DESIGNER persona 已更新）

### 不采纳/缓议
- pre-commit hook 查 Reviewer PASS 标记（Reviewer 提议）：对 wrapper 环境过重，以规则 11 文字 + CEO 统一 push 覆盖。
- TASKS 开标时间戳 biocheck（Dev 提议）：过度工程，以 A2 的「PRD 触达条款 + 验收条件先行」覆盖。

## 23:54 — 晚间复盘（T31–T34 + B5 完结 + 归档与报告方法论）

### 做了什么
- T31：A3 行动项落地（fallback 门槛口径统一，211→216 单测）。
- B5 完整生命周期：T32 侦察（全量脚本证伪「visit↔activity 重叠」原假设）→ 分支实验模式决策 → RESEARCH-B5 讲解式报告 → T33 实验分支（方案 A：过滤 timelinePath-only traces，三角归零+零孤立，**净改善**）→ Reviewer PASS → merge。
- T34 归档：T1–T29 移入 ARCHIVE.md。
- 报告方法论升级：「可信度论证（bias direction）」概念（用户倡议）→ 新模板 EXPERIMENT.md + EXPERIMENT-B5 v2 回补。

### 什么做得好（保留）
- **分支实验模式完整跑通**：main 对照组 + `experiment/<slug>` 实验组 + 用户本地真数据人肉 A/B 拍板——首轮落地就成功终结 B5（假设被证伪 → 不放宽、不硬做，转向真实盲区）。机制本身有效，值得升为通用流程。
- **假设证伪后被诚实修正**：侦察 §5.2「15-20% 缺口」被实测推翻后主动改口（量纲陷阱），不维护错误结论。
- **用户驱动的方法论改进直接落地**：可信度论证从「用户指出的理念」到模板规范仅两小时（新模板 + 回补 v2）。
- **报告「讲解式」重写**：RESEARCH-B5 从数据快照改为故事课结构，用户认可（「知识才传得下去」）。

### 什么做得不好（要改）
- **归档语义歧义**（用户纠正）：Dev 把 TASKS→ARCHIVE 归档做成**一行摘要**，丢失验证数据/Reviewer 修正/决策记录；模板只说「保留结构化历史」未明说「完整搬运」。返工一轮（ARCHIVE.md 改回原文完整搬移）。
- **thinking 没进文档**（用户指出，本次核心教训）：T33 的「isolated=0 可信度论证」（无坐标 visit 口径 ± 两向推演、E7 坐标方向、other=0 反证）只在 thinking 里跑完就被折叠成一句话；报告缺「为什么敢信」的推理链。→ 已用 EXPERIMENT.md §三 模板化。
- **CEO 文档编辑事故**：往 DECISIONS.md 加新条目时误覆盖旧条目标题行（插入位置错）——追加制文档的「插头」位置必须核查。
- **实验分支 Dev 交付 0 commits**：宽松 Review 模式下 T33 返回时代码全未提交（实验分支未 push），靠 CEO 检查补 commit。分支模式下「可追溯性」仍需盯。

### 下次怎么改（行动项，CEO 拍板，用户已确认方向）
- [x] **A6 归档=完整搬运**：ARCHIVE.md 模板 + WORKFLOW 明写「归档即完整搬移（非摘要）——保留原文全部验证数据/Reviewer 修正/决策记录」；TASKS Done 区只留近 N 条看板（已完成模板+WORKFLOW 同步）
- [x] **A7 实证报告含 §可信度论证**：`templates/project/docs/EXPERIMENT.md` 新增「§三 可信度论证（bias direction）」——每疑虑「差异→方向→判定」固定格式 + 必查清单（口径/坐标/抽样/量纲/子集/自洽）；侦察/实验/压测报告必须含此节（已完成模板建立 + EXPERIMENT-B5 v2 回补）
- [ ] **A8 分支实验模式升为通用流程**：把「main 对照组 + experiment/<slug> + 用户真数据人肉 A/B + merge 门控走完整流程」写进根 repo WORKFLOW（不只停留在项目 DECISIONS）——T33 已验证有效
- [ ] **A9 追加制文档编辑纪律**：DECISIONS/NOTES/records 等追加制文件「只追加在文件末尾，不插入中间」；或新条目置顶时保留原标题（CEO 自己 23:00 误覆盖旧条目，教训自记）
- [ ] **A10 实验分支可追溯性**：分支实验模式规则补一条「实验分支 commit 即时性」——实验期宽松 Review 但代码必须随做随 commit，交付时至少分支上可追溯（T33 0 commits 教训）

### 不采纳/缓议
- （无新增）