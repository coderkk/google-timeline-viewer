# Retros — 2026-09-16

> 每日文件：同一天多次检讨追加 `## HH:mm — 主题` 节。

## 13:10 — v1.0.0 发布 Retro（T35–T40 发布周期）

### 做了什么
- **T35** 导入进度修复：单文件语义（去 multiple）+ 不确定动画进度（诚实原则，不假造百分比）+ `parseProgress` 删除——用户实测静止 0% 触发；PRD v1.22 + 功能 14 立项
- **T36** 合并归档页（功能 14）：语义段取最新 + rawSignals 窗口互补累积（±60s+100m 折叠），独立 `/app/merge` 页 + merge.worker + >200MB 护栏——用户拍板「不堆 N 份完整 Timeline.json」；PRD v1.23
- **T37** raw 点渲染压缩测（15k 窗口）：p95 183ms / 43fps 放行，产品零修改；DATA-FINDINGS §10；附带「范围切换 2.3s 冻结」入 Backlog
- **T38** 发布前收尾：README/i18n 单文件化 + 「合并已实现」表述清理 + 全仓 grep 零命中 + 双视口 19 项×2 冒烟——**抓到 N1：mobile `_leaflet_pos` 竞态复现率 3/4**（与历史「偶发」记录矛盾）
- **T39** 发布前终批：N1 根治（真根因=Leaflet `_onZoomTransitionEnd` timer 在 `map.remove()` 后触发 → `_animatingZoom=false` 字段复位）→ **Reviewer 两次 REJECT**（①Places 同族漏覆盖 6/6 ②封印脚本 S1 server 泄漏假信号 + G1 吞败）→ 共享 hook + 双视图 + 封印脚本 `smoke-race-check.mjs` 入库；Landing 第 4 卡「合并归档」；PRD v1.24
- **T40** v1.0.0 发布：GitHub Pages 部署（push 自动触发，success）+ 线上双视口 14/14 冒烟 + bundle 特征核对 + tag `v1.0.0` + CHANGELOG。发布地址 `https://coderkk.github.io/google-timeline-viewer/`

### 什么做得好（保留）
- **封印脚本当成产品一样对待**（Dev/Reviewer 共识）：退出码门控 + `--port 0` + 进程组 kill + `navAt<250ms` 判别性判据（WIDE 轮也计败，杜绝空转 PASS）+ 故障注入验证——关键认知「验证资产自身不可靠 = 假信号，比没有门禁更危险」值得固化
- **REJECT 的判别性价值实证两次**（Reviewer）：①「根治」声明被 Places 探针否证（修对单实例 ≠ 族被根治）②门禁假信号被复验抓出（假 FAIL 6/6 + 假 PASS 0/6 都亲历）——**REJECT 不是损耗，是免费的边界探测**（本轮价值最高的单次输入）
- **诚实原则在压力下立住**（Dev）：T37 报告被抓出「longtask 0」与数据矛盾后不是改历史，是 §10 如实化（如实记 11/16、647ms）；T35 进度条不假造数字——**发布周期里最贵的资产**
- **失败经验写成纪律而非删掉**（Dev/Reviewer）：T27 `map.stop()` 白屏 → hook 文件头写两中招史 + 「unmount 永不调 map 方法」注释 → T39 纯字段复位落地；N1 家族三中招后靠共享 hook + 封印脚本从 patch 升级为防线
- **N1 家族结构性消除**：共享 hook `useResetZoomAnimOnUnmount` 双视图同挂 + SMOKE-CHECKLIST B 段「发布前必跑」——第三处再漏在结构上被消除

### 什么做得不好（要改）
- **验证脚本不入库**（Dev/Reviewer 共识，最高频缺陷）：`smoke-t38.mjs`（19 项×2 视口）、`repro-n1c.mjs`、`verify-n1-fixed.mjs`、`reviewer-probe-places-race3.mjs` 全程流浪 `/tmp/opencode/`，轮次结束即蒸发。后果：3/4 复现路径无法复跑；T39 之前没有任何一条命令能反查 N1；门禁脚本是 REJECT 后才「事后封」的——**发布周期 90% 时间无验证资产沉淀**
- **根治声明过宽 + 只修同族半边**（T39 首轮核心问题）：Dev「所有路径都被覆盖」只论证了 Trips，Places `RadiusCircle` ring-fit 同根因漏掉（6/6 复现）。模式：「认定一个根因就停止扫族」——T27 `map.stop()` 防 N1 时同样只盯 FitController 一个点
- **「已知噪音」记录无量化、无复核时效**（Reviewer F3）：N1 从 T27 记为「非阻塞已知 console 噪音」→ 2 周内被 T38 实测 3/4 否证。旧记录只有定性（「偶发」）、无复现率分母、无「何时过期/触发条件」——读到旧结论的人都会按「可放行」处理
- **T37 性能报告自测不足**（Dev 3）：`perf-browser.mjs` 聚合 bug（`push(...m.longtasks ?? [])` 恒推空 → summary longtaskMax 恒 0）→ 报告写「longtask 0」；`s2.panoPans` 从未入库，§10.2 引用查无实据——**数据型交付物必须「脚本输出 ↔ 报告引用 ↔ 入库数据」三方对账**
- **多文件语义一次变更切了 3+ 刀**（Dev 4）：T35 清 ImportPanel → Reviewer 抓 dropHint → 指派清 help → T38 再清 README + i18n + 全仓 grep——**语义变更第一刀就该拉全清单一次清完**（T38 的全仓 grep 审计本该在 T35 做）
- **PRD 内部一致性漏检**（CEO）：功能 10（GeoJSON/KML 导出）与「不做」区描述冲突，发布遗留——发布门面核对缺「PRD 内部一致性」项

### L1 判定偏差复盘（本轮校准焦点——Dev 与 Reviewer 有观点分歧）
- **T38 定 L1，后验**：Dev 认为应升 L2（改 >1 文件 / 门面文案 / 需要冒烟三中全中）；Reviewer 认为 T38 按 L1 可接受，真正偏差不在档位——**L1 免冒烟全集与发布前体检是两个正交维度，混在一个档位里才打架**（N1 是 T27 起带载 2 周的存量病，被「发布体检」撞到，不是 T38 改出来的）
- **T39 定 L2、T40 定 L1**：双方无异议，判定合理
- **本轮偏差率**：1/4（T38 争议），集中在「发布链任务」这一特殊形态

### 下次怎么改（行动项，CEO 拍板采纳）
- [ ] **A10 验证脚本入库制度化**：新验证/复现脚本默认落 `scripts/smoke-*.mjs` 命名空间并 commit（临时 probe 用 `scripts/scratch/`，轮次结束转正或删除并在 NOTES 记处置）；SMOKE-CHECKLIST 每项必须指到 `scripts/` 内可复现入口。**验收**：下一发布周期结束 `grep '/tmp/opencode' docs/` 零命中；T38 19 项双视口冒烟收编为 `scripts/smoke-release.mjs`（Dev 承诺）
- [ ] **A11 根治型修复声明强制附同族清单**：NOTES 出现「根治/不会再犯/不复发」措辞 → 三件套：①同族调用点清单（grep 全部同类触发源 + 逐视图挂载路径）②每个成员至少一条判别性验证记录（含窗口判据如 `navAt<250ms`，非空转）③结构性消除手段（共享 hook/封印脚本）而非逐点 patch。**验收**：下一次含「根治」措辞的任务，Reviewer 对照清单复核，缺任一成员即 REJECT（同 T39 判据）
- [ ] **A12 「已知噪音」记录量化 + 时效**：所有「非阻塞/已知/偶发」记录必须带：①复现率（分母≥4，如 3/4）②触发条件 ③复核时限（如「下次发布前复核」）。**验收**：发布周期末 grep DECISIONS/NOTES 中「偶发/已知噪音」，无分母/无复核时限者清零
- [ ] **A13 L1 冒烟豁免加例外条款**（Reviewer 校准建议）：①**发布链例外**：发布前收尾/终批/发布验证链任务（T38/T39/T40 形态）不论档位执行冒烟全集；②**矩阵例外**：SMOKE-CHECKLIST 标准项保护存量功能，任何档位都要跑——豁免只针对「本次改动引发的回归冒烟」，不豁免既定矩阵。一句话：**L1 豁免的是 Diff 性审查的回归义务，不是发布门禁**；并将 N1 这类移动首访交互路径补入 SMOKE-CHECKLIST 标准矩阵（T30 移动端适配时就该进）
- [ ] **A14 同族枚举前置**（Dev 承诺 + A11 合并）：任何时序/竞态/异步泄漏类 bug，动手前在 NOTES 写「同族排查清单」，提交审查时附上——**验收**：下次此类修复 Review 第一轮 0 次「同族遗漏」打回
- [ ] **A15 数据型交付物三方对账**（Dev 3）：性能/侦察报告 = 「脚本输出 ↔ 报告引用 ↔ 入库数据」三方核对后才可发布结论——复用 T37 §10 执行方式为默认流程

### 不采纳
- Reviewer「T38 该按命令强度跑而非升档标签」与 Dev「T38 应 L2」的取向——二者机制诉求已并入 A13（发布链例外），档位标签之争不单独采纳；判定一致：文档类含冒烟的发布链任务归 L2 更清晰，后续拆卡默认执行

### 遗留（发布后持续项）
- PRD 功能 10（GDP/KML 导出）描述核对 → Backlog 候选（CEO 负责，不阻塞）
- 封印脚本 WIDE 阈值负载脆（发布常带负载会假 FAIL，诱使「重跑至绿」）→ 建议项留档：页内 `performance.now` 净时序 / 重试
- Tag `v1.0.0` 发布后首次公开访问数据（GitHub Pages 无埋点——用 repo traffic 观测公开分享触达）

---
**参与**: CEO 主持，Dev + Reviewer 反馈。整理: CEO 2026-09-16 13:10。