# Brainstorms — 2026-09-15

## 06:40 — google-timeline-viewer：下一步与未完成盘点

### 现状（已上线）
- 功能 1–9 基本落地并部署：四格式导入、日期筛选（双月历）、Trips（时间轴 / 按活动类型）、Places、隐私设置、教程页、Landing、示例数据、更换数据
- 近期完成 T16–T19：时间轴路线回退语义段、逐点真实时间、轨迹去重、点选 GPS 弹窗 + Google Maps 链接、左侧时间线、双月历范围选择器、更换数据
- 线上：https://coderkk.github.io/google-timeline-viewer/（live bundle `index-BVLzNuVM.js`）

### 未完成清单

#### A. 未结案的产品问题（影响现有体验）
- **A1 [等用户决定]** 跨午夜记录：选 2025-01-30 出现 2025-01-29（overlap 语义）。方案：**A** 标示跨夜 / **B** 改为「起点在范围内」/ **C** 记录保留但路径点裁剪到范围
- **A2 [P2]** 渲染性能：raw 20k / route 30k 点 CircleMarker（canvas 兜底）—— 发布前用真实 15k 窗口压测，定降 cap 或分层预算
- **A3 [advisory]** `preparedTimeline` 在 activityType 模式下也重算 route（线性，可懒算）
- **A4 [advisory]** 测试缺口：DST 日界、route cap→downsampled 传播、跨零点排序

#### B. 发布面 / 完整度
- **B1** PRD 功能勾选全未更新（9 个功能 0 勾）—— 文档卫生
- **B2** README 截图可能仍是旧 UI（时间轴 / 双月历 / 左侧时间线未入图）
- **B3** 公开释出检查（Landing 文案、隐私声明、示例数据、版本 tag）
- **B4 [KIV]** 移动端适配 —— 公开分享后手机流量占比

#### C. 新功能（Backlog，全 P2）
- **C1** 行程统计报表（总距离 / 日均运动量 / 地点频次）
- **C2** 行程分享 / 导出（GeoJSON / KML / 截图）
- **C3** 多语言（英文为主，可换中文）
- **C4** 跨设备多 Takeout 合并去重（含 rawSignals 滚动窗口互补）
- **C5** 离线瓦片 / 自托管瓦片服务器
- **C6** livedata 完整支持（visit 段与 activity 段关联展示）

### 待用户决策
1. **A1 选 A / B / C？**（CEO 建议 **A+C**：停留记录保留并标「跨夜」，但路径点裁剪到选定范围内 → 记录不丢、地图不越界）
2. **下一阶段目标**：**发布 / 分享（portfolio 拿得出手）** 还是 **自用功能深化**？
   - 发布面 → B2 / B3 / B4 优先
   - 自用 → C1 / C2 优先
3. **现在做性能压测（A2）吗？**（发布前建议做）

### CEO 建议排序
1. **A1**（你拍板，半天内可完成）
2. **B2 + B3**（README / 释出检查，让 portfolio 拿得出手）
3. **A2**（性能压测，发布前）
4. **C1 或 C2**（视目标而定）

### 讨论点
- 这个项目的定位是「**给大众免费用的 portfolio 作品**」（PRD 目标）——那么**移动端 + README/释出**的优先度应高于新功能；但若主要是**自用**，C1 统计报表最实用。
- A2 性能问题在「全部」视图（10 年）最明显：route 30000 点 + 圆点。是否接受降采样，或做分层渲染？

---

## 06:55 — 团队讨论（Designer / Security Engineer / Writer / Dev / Reviewer）

CEO 主持，各角色就「下一步与未完成」给意见（Persona 见 `.opc/personas/`）。

### Designer（UI/UX）
- **B4 移动端应升 P1（不是 KIV）**：portfolio 第一印象常是手机点开。双月历在 375px 不可用 → 移动端降为**单月历**、快捷档前置；左时间线改**底部抽屉**；地图全屏；触控目标 ≥44px。
- **A1 选 A+C，但要显式化**：范围外但跨入的停留，列表/marker 用**虚线框 +「跨夜 · 自 1-29」**；路径点裁剪。反对 B（丢真实数据）。
- **先 C2 再 C1**：分享/导出（截图带日期+轨迹+统计角标）服务 portfolio 传播；顺手把总距离/天数做成导出图 stat card。
- 其他：空态与「0 结果」要区分；大文件解析要有进度反馈；明暗主题下蓝/红对比度 QA。
- 排序：**B4 → A1(A+C) → B2/B3 → C2**

### Security Engineer
- **BLOCKING（公开释出前）— Google Maps 链接与「数据不出设备」矛盾**：`googleMapsUrl()`（TripMap.tsx:132、PlacesMap.tsx:184）把精确坐标放进 URL 送 google.com，点击还泄漏 IP + Referer。功能 5 验收「无请求携带坐标」只对 JSON 成立。需**明确披露**「坐标将发送给 Google」或默认「复制坐标」+ 外链 opt-in；`rel="noopener noreferrer"` 保留。
- **B3 检查清单**：CSP 完好、无 source map、repo/ **git history** 无真实 Takeout 数据、示例仍标「模拟」、`npm audit`、确认无 localStorage/IndexedDB 写入。
- **C2 护栏**：确认弹窗（列出范围与内容）、只导出当前筛选视图、剥离原文件名/装置/路径 metadata、不做链接分享（绝不上传）、明文警告、不自动下载。
- **B4**：截图/分享警告；不自动复制坐标。**C4**：不得引入任何存储。**C5**：离线瓦片反而更隐私。

### Writer（文件/Portfolio）
- **B1 PRD 勾选**：9/9 未勾，对「文件驱动流程」的卖点是自相矛盾；修法=勾 1–9 并附 T 编号，半天内。
- **B2 README（高优先，且有真 bug）**：7 张 PNG 全是 09-13（早于 T15–T19），`trips.png` 早于时间轴模式；`landing-hero.png` 与 `landing-builtwith.png` **字节完全相同**（其一错）；README 引用裸檔名但实际在 `docs/screenshots/` → **GitHub 上图片全裂**。
- **B3**：demo 链接还是 `<user>` 占位、badge `(#)` 占位、**没有 LICENSE 档**却宣称 MIT。
- P0：修 README 图片路径、重拍截图、删重复 hero；补 LICENSE；填 URL/badge。P1：勾 PRD、「What's new」。P2：CHANGELOG + `v1.0` tag（值得做）。

### Dev（技术/工时）
| 项 | 工时 | 风险 | 备注 |
|---|---|---|---|
| A1-A 标示跨夜 | S | 低 | 纯展示 |
| A1-B 改起点语义 | S | 中 | 会丢 01-30 早上停留，反对 |
| **A1-C 路径点裁剪** | **M** | 低-中 | **最干净**：`buildTimelineRoute` 已算好每顶点 `sortMs`，在 sort 后、去重前按 range 过滤 ~10 行 |
| A2 渲染性能 | S（降 cap）/ L（分层） | 中-高 | 30k CircleMarker = 30k Leaflet layer，真实风险；先降 cap 压测；clustering 需插件（违背零依赖） |
| A3 route 懒算 | S | 低 | 按 mode 分 memo，但下游守卫点多 |
| A4 测试缺口 | S | 低（DST 真隐患） | `endOfDayMs`/`fitKey` 假设每天 24h |
| C1 统计报表 | M | 低 | 数据齐全 |
| C2 导出 | M（GeoJSON/KML）/ L（截图） | 中 | 先做 GeoJSON/KML |

### Reviewer（质量/债务）
- **S**：A2 未压测（公开释出会撞手机流量）；A1 语义未决；**`coveredByRaw` 过粗**（段跨度内命中**一个** raw 点就整段丢弃语义路径 → 30 天保留窗边界处长段可能被误判「已覆盖」→ 路线空洞，**需 Dev 复核**）；B1/TASKS 陈旧（`<!-- next: T18 -->` 但 T18/T19 已 Done）。
- **A**：A3 白算；A4/A5（route cap→downsampled 传播影响 summary 诚实性）；**DECISIONS 缺口**（T18/T19 无条目；T14 条目已被 T14.3 推翻却无 superseded 标记；T16 决策仍写「按本地日分桶」与 T17 实际「按时间合并」相左）。
- **Must fix before release**：A2 压测、A1 拍板、`coveredByRaw` 复核、PRD/TASKS 同步、T18/T19 DECISIONS 补录。

### 共识与分歧
- **共识**：A1 采 **A+C**（记录保留并标跨夜、路径点裁剪）；A2 发布前必须压测；PRD/TASKS/README 需同步。
- **分歧**：C1 vs C2 优先——Designer 主张 **C2 先**（传播价值），Writer 认为 B2/B3 先；CEO 倾向先把「发布面」做扎实（B4/B2/B3 + Security 的披露项），再做 C2。
- **新发现（先前遗漏）**：① Google Maps 外链的隐私披露（Security，阻塞）；② `coveredByRaw` 边界正确性（Reviewer，需复核）；③ README 图片路径全裂 + 无 LICENSE（Writer，真 bug）。
